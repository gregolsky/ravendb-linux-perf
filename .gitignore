RavenDB/
RavenData/
FlameGraph/
*.tgz
*.tar.bz2
*.tar.gz
*.data
*.dump
*.map
*.folded
*.svg
*.nettrace

# Loose capture artifacts — normally inside a bundle, but can land in the cwd
# when a bcc/dotnet tool is run directly in the repo. Example copies are
# re-included below so committed samples stay tracked.
memleak.txt
biolatency.txt
biosnoop.txt
bitesize.txt
cachestat.txt
ext4slower.txt
fileslower.txt
runqlat.txt

# Track committed example outputs
!examples/
!examples/*.svg
!examples/*.txt
